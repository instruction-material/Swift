import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD12 Navigation and Multi-Screen Apps supplemental 2")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
