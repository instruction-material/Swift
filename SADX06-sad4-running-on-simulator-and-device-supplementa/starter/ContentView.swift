import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD4 Running on Simulator and Device supplemental 2")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
