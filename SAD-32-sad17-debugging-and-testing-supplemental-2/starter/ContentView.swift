import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD17 Debugging and Testing supplemental 2")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
