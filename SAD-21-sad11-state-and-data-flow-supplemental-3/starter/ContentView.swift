import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD11 State and Data Flow supplemental 3")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
