import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SADX Enrichment and Legacy Archive Boundaries supplemental 3")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
