import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD3 Certificates, Signing, and Bundle IDs supplemental 2")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
