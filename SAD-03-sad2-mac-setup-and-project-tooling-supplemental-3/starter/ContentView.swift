import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD2 Mac Setup and Project Tooling supplemental 3")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
