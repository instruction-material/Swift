import SwiftUI

struct ContentView: View {
	var body: some View {
		VStack(spacing: 16) {
			Text("SAD16 Persistence supplemental 3")
			Text("TODO: replace this with the module UI.")
		}
		.padding()
	}
}
